# 🚀 Telegram MiniApp 手动部署完整指南

## 📋 第一步：在本地创建项目

### 1.1 创建GitHub仓库（如果还没有）
1. 访问 https://github.com/reportyao
2. 点击 "New repository"
3. 仓库名：`telegram-lottery-miniapp`
4. 设为Public
5. 不初始化README、.gitignore或license（我们已经有了）
6. 点击 "Create repository"

### 1.2 克隆到本地
```bash
git clone https://github.com/reportyao/telegram-lottery-miniapp.git
cd telegram-lottery-miniapp
```

### 1.3 创建package.json
在项目根目录创建 `package.json` 文件：

```json
{
  "name": "telegram-lottery-miniapp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build",
    "start": "next start -p 3000",
    "lint": "next lint",
    "type-check": "tsc --noEmit",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:ci": "jest --ci --coverage --watchAll=false"
  },
  "dependencies": {
    "@radix-ui/react-dialog": "^1.0.5",
    "@supabase/supabase-js": "^2.39.0",
    "@telegram-apps/sdk": "^1.1.0",
    "clsx": "^2.1.0",
    "lucide-react": "^0.344.0",
    "next": "^14.2.33",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "tailwind-merge": "^2.2.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.1.4",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^14.5.1",
    "@types/jest": "^29.5.5",
    "@types/node": "^20.0.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "eslint": "^8.57.0",
    "eslint-config-next": "^14.2.33",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.0.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "setupFilesAfterEnv": [
      "<rootDir>/jest.setup.js"
    ],
    "moduleNameMapping": {
      "^@/(.*)$": "<rootDir>/$1"
    },
    "transform": {
      "^.+\\.(ts|tsx)$": "ts-jest"
    },
    "testMatch": [
      "**/__tests__/**/*.(test|spec).(ts|tsx)",
      "**/*.(test|spec).(ts|tsx)"
    ]
  }
}
```

### 1.4 安装依赖
```bash
npm install
```

## 📁 第二步：创建目录结构

### 2.1 创建主要目录
```bash
mkdir -p app components hooks lib types public __tests__
mkdir -p app/api
mkdir -p app/admin
mkdir -p app/my-resales
mkdir -p app/orders
mkdir -p app/posts
mkdir -p app/profile
mkdir -p app/referral
mkdir -p app/resale-market
mkdir -p app/topup
mkdir -p components/ui
mkdir -p __tests__/api
mkdir -p __tests__/components
mkdir -p __tests__/hooks
mkdir -p __tests__/lib
mkdir -p __tests__/utils
mkdir -p bot
mkdir -p docs
mkdir -p locales
```

## 📝 第三步：获取文件代码

**等待我为您提供每个文件的代码...**

## 🔄 第四步：提交到GitHub

### 4.1 添加所有文件
```bash
git add .
```

### 4.2 提交
```bash
git commit -m "初始提交：Telegram Lottery MiniApp完整项目"
```

### 4.3 推送到GitHub
```bash
git push origin main
```

## 🎯 接下来需要什么？

请告诉我您已经完成了哪一步：
1. ✅ 创建了GitHub仓库
2. ✅ 克隆到本地
3. ✅ 创建了package.json
4. ✅ 安装了依赖
5. ✅ 创建了目录结构

然后我会为您提供每个文件的具体代码！