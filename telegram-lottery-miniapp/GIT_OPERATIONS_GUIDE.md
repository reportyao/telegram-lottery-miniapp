# 🔄 Git操作指南

## 项目已成功推送到GitHub
**仓库地址**: https://github.com/reportyao/telegram-lottery-miniapp

---

## 📋 常用Git命令

### 1. 拉取最新代码
```bash
# 切换到项目目录
cd telegram-lottery-miniapp

# 拉取最新代码
git pull origin main
```

### 2. 查看项目状态
```bash
# 查看当前状态
git status

# 查看提交历史
git log --oneline -10

# 查看文件差异
git diff
```

### 3. 提交更改
```bash
# 添加所有更改
git add .

# 提交更改
git commit -m "你的提交信息"

# 推送到GitHub
git push origin main
```

### 4. 分支操作
```bash
# 创建新分支
git checkout -b feature/新功能名称

# 切换分支
git checkout main

# 合并分支
git merge feature/新功能名称
```

---

## 🔧 推荐的开发工作流

### 1. 日常开发流程
```bash
# 1. 开始工作前，拉取最新代码
git pull origin main

# 2. 创建功能分支（可选）
git checkout -b feature/你要开发的功能

# 3. 进行开发和测试
# ... 你的开发工作 ...

# 4. 提交更改
git add .
git commit -m "feat: 添加新功能描述"

# 5. 推送到远程分支
git push origin feature/你要开发的功能

# 6. 在GitHub创建Pull Request（如果使用PR流程）
```

### 2. 快速修复流程
```bash
# 1. 拉取最新代码
git pull origin main

# 2. 在main分支直接修复
# ... 修复工作 ...

# 3. 提交和推送
git add .
git commit -m "fix: 修复bug描述"
git push origin main
```

---

## 🚨 冲突解决

### 当Git拉取或推送时遇到冲突：

```bash
# 1. 查看冲突文件
git status

# 2. 手动解决冲突文件中的冲突
# 冲突标记: <<<<<<< , ======= , >>>>>>>

# 3. 标记冲突已解决
git add 解决后的文件名

# 4. 完成合并
git commit -m "resolve: 解决冲突描述"

# 5. 推送到远程
git push origin main
```

---

## 🔄 代码回滚

### 回滚到之前的提交
```bash
# 查看提交历史
git log --oneline

# 回滚到指定提交（谨慎使用）
git reset --hard 提交哈希值

# 强制推送到远程（谨慎使用）
git push origin main --force
```

---

## 📁 文件管理

### .gitignore 配置
确保以下文件不被Git跟踪：
```
# 环境变量
.env
.env.local
.env.production

# 构建输出
.next/
out/
dist/

# 依赖
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE配置
.vscode/
.idea/
*.swp
*.swo

# 操作系统
.DS_Store
Thumbs.db
```

### 恢复被忽略的文件
```bash
# 强制添加被忽略的文件
git add -f 文件名

# 或编辑.gitignore文件后添加
git add -f .env.example
```

---

## 🌟 项目结构说明

```
telegram-lottery-miniapp/
├── app/                    # Next.js 14 App Router
│   ├── api/               # API路由
│   ├── admin/             # 管理页面
│   ├── layout.tsx         # 根布局
│   └── page.tsx           # 首页
├── components/            # React组件
├── lib/                   # 工具库
├── supabase/              # Supabase配置
├── types/                 # TypeScript类型
├── public/                # 静态资源
└── docs/                  # 文档
```

---

## 🏷️ 提交信息规范

### 推荐格式
```
type: 简短描述

详细描述（可选）

- 修复的问题1
- 修复的问题2
- 新增的功能1
```

### 提交类型
- **feat**: 新功能
- **fix**: bug修复  
- **docs**: 文档更新
- **style**: 代码格式调整
- **refactor**: 代码重构
- **test**: 测试相关
- **chore**: 构建或工具配置

### 示例
```bash
git commit -m "feat: 添加彩票购买功能

- 实现彩票选择界面
- 添加购买确认流程  
- 集成支付接口

Closes #123"
```

---

## 🔐 安全注意事项

### 1. 敏感信息
- 永远不要提交 `.env` 文件
- 使用 `.env.example` 共享配置模板
- API密钥通过环境变量管理

### 2. 访问控制
- 定期更新GitHub访问令牌
- 使用SSH密钥而不是用户名密码
- 启用两步验证

---

## 📊 监控和备份

### 1. 定期备份
```bash
# 创建项目备份
tar -czf telegram-lottery-backup-$(date +%Y%m%d).tar.gz telegram-lottery-miniapp/

# 上传到云存储（可选）
# aws s3 cp telegram-lottery-backup-20251103.tar.gz s3://your-backup-bucket/
```

### 2. 代码审查
- 在合并前审查所有更改
- 使用Pull Request流程
- 确保测试通过

---

## 🆘 紧急操作

### 恢复误删文件
```bash
# 查看文件历史
git log -- 文件名

# 恢复文件到之前的版本
git checkout 提交哈希值 -- 文件名
```

### 完全重置项目
```bash
# 危险操作：完全重置到远程状态
git fetch origin
git reset --hard origin/main
git clean -fd
```

---

**💡 提示**: 定期备份重要代码，遵循Git最佳实践！

**仓库地址**: https://github.com/reportyao/telegram-lottery-miniapp